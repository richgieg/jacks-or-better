/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // TODO: Uncomment when it's time to build for Google Play and App Store.
  // output: "export",
}

module.exports = nextConfig
